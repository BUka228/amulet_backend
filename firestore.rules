rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Разрешаем читать и писать в любую коллекцию,
    // ТОЛЬКО если пользователь аутентифицирован.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}